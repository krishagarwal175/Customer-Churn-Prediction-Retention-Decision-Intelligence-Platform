# Runtime dependencies for the Vercel FastAPI deployment (kept slim).
# Heavy analytics/offline libraries (scikit-learn, xgboost, shap, plotly,
# matplotlib) live in requirements-dev.txt and never ship to the serverless
# runtime — the API serves precomputed artifacts and a lightweight NumPy model.
fastapi>=0.110
pydantic>=2.6
numpy>=1.26
pandas>=2.2
jinja2>=3.1
