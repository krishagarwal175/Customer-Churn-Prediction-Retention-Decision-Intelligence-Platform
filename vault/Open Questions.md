# Open Questions

- Maturity of downstream modules (`models/`, `features/`, `explainability/`, `simulation/`, `reporting/`, Streamlit `app/`) — files exist but not yet reviewed/verified.
- Should `schema.eda` also feed feature engineering / reporting to fully centralize the semantic mapping? (Currently EDA-only.)
- Is `docs/` (data_dictionary, model_card, etc.) kept in sync with the active dataset schema?

See [[Home]]
